Install procedure requires Cmake (http://www.cmake.org)
Once obtained, create a subdirectory 'build':
	$ cd build
	$ cmake ..
	$ make
	# make install
The program can then be run as 'ncrok'

Dependencies - dev and runtime packages for all:
	taglib
	gstreamer-0.10
	ncurses
